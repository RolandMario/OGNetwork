# Expo environment variables (SDK 49+ reads .env natively; prefix must be EXPO_PUBLIC_)
#
# Point the app at the PRODUCTION backend on Vercel.
# This overrides the dev defaults (localhost / 10.0.2.2) from src/constants/apiConfig.js.

EXPO_PUBLIC_API_URL=https://og-network-backend.vercel.app